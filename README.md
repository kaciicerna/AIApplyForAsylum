# AIApplyForAsylum

- WINDOWS
  ```bash
  python3 -m venv .venv
  .\.venv\Scripts\activate
  ```
  - MacOS
  ```bash
  python3 -m venv .venv
  source .venv/bin/activate
  ```

2. Instalace závislostí
```bash
pip3 install -r requirements.txt 
```
